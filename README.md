RedGate.GitHub.Api
==================
This is a very incomplete implementation of the GitHub v3 API.

It's likely to remain very incomplete, with only the bits of the API we actually need added, on an as-needed basis. Feel free to add more!